# Copie para .env.local ao testar localmente (nunca commite .env com chaves reais).
# No Vercel: Settings → Environment Variables → use apenas esta variável.
# Chave em https://openrouter.ai/keys

OPENROUTER_API_KEY=
