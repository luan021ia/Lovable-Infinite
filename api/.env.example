# Copie para .env.local ao testar localmente (nunca commite .env com chaves reais).
# No Vercel: Settings → Environment Variables.

# Open Router (improvePrompt)
OPENROUTER_API_KEY=

# validateLicense/createPanelUser: Firebase Admin (JSON do service account, uma linha)
# Firebase Console → Configurações do projeto → Contas de serviço → Gerar nova chave privada
FIREBASE_SERVICE_ACCOUNT_JSON=

# Realtime Database URL (opcional; fallback usa project_id do JSON)
# Ex: https://lovable2-e6f7f-default-rtdb.firebaseio.com
FIREBASE_DATABASE_URL=

# createPanelUser: e-mails master (separados por vírgula), opcional
MASTER_EMAILS=luan93dutra@gmail.com

# ========== SEGURANÇA JWT (OBRIGATÓRIO) ==========
# Segredo para assinar tokens JWT. DEVE ser uma string longa e aleatória.
# Para gerar: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# IMPORTANTE: Se você mudar este valor, TODAS as sessões existentes são invalidadas!
JWT_SECRET=

# ========== WEBHOOK N8N (OBRIGATÓRIO) ==========
# URL do webhook n8n (antes ficava no client-side, agora SÓ no servidor)
N8N_WEBHOOK_URL=

# Salt e chave de scramble do protocolo PROMPTXV2
N8N_SECRET_SALT=
N8N_SCRAMBLE_KEY=
