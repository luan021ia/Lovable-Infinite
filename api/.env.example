# Copie para .env.local ao testar localmente (nunca commite .env com chaves reais).
# No Vercel: Settings → Environment Variables.

# Open Router (improvePrompt)
OPENROUTER_API_KEY=

# createPanelUser / listPanelUsers / updatePanelUser / deletePanelUser: Firebase Admin
# JSON do service account (uma linha). Firebase Console → Contas de serviço → Gerar nova chave privada
FIREBASE_SERVICE_ACCOUNT_JSON=

# Realtime Database URL (opcional; padrão: https://PROJECT_ID-default-rtdb.firebaseio.com)
# FIREBASE_DATABASE_URL=

# E-mails master (separados por vírgula), opcional
MASTER_EMAILS=luan93dutra@gmail.com
